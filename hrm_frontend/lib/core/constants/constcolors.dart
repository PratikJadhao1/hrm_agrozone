import 'package:flutter/material.dart';

class Constcolors {
  static const normalTextColor = Colors.white;
  static const backgroundColor = Colors.black87;
  static const selectedIconColor = Colors.amber;
  static const normalIconsColor = Colors.white;
  static const navigationBarBackColor = Colors.black;
  // static const clickedColor = Color.fromARGB(255, 30, 68, 131);
}
